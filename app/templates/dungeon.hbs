{{#bs-button-group
  type="radio" 
  as |bg|}}
  	{{#each model as |dungeon|}}
 		{{#bg.button onClick=(action "loadDungeon" dungeon.url)}}<img src={{dungeon.img}}>{{/bg.button}}
  	{{/each}}
{{/bs-button-group}}


{{outlet}}